/**
 * Paket für Klassen, die die Business-Logik des Systems darstellen.
 * <p>
 * In diesem Package werden die Klassen abgelegt, die die eigentliche Logik
 * des Banksystems darstellen. Das sind also die Klassen, die für die
 * Darstellung der Bank, der Kunden und der Konten benötigt werden.
 */
package de.raywo.tutorials.bank.logic;
